# 只 COPY 明确列出的配置/lua/vendor,其余一律排除,缩小构建上下文
.git
.gitignore
k8s/
test/
bin/
# ⚠️ 不排除 bodylog-listener-go/:CI 用 classic builder 时 `-f Dockerfile.bodylog .` 也读【根】.dockerignore
#   (per-Dockerfile 的 Dockerfile.bodylog.dockerignore 仅 BuildKit 生效)。排除它会让 bodylog 的
#   `COPY bodylog-listener-go/go.mod` 失败。主 openresty 镜像是 selective COPY(不 COPY 它),含进上下文无害。
bodylog_http_token
*.md
*.html
__pycache__/
*.pyc
*.swp
